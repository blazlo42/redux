export const INCREMENT = () => {
  return { type: "INCREMENT" };
};

export const DECREMENT = () => {
  return { type: "DECREMENT" };
};

export const LOGGED = () => {
  return { type: "SIGN_IN" };
};
